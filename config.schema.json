{
  "pluginAlias": "AcHttpPlatform",
  "pluginType": "platform",
  "singular": false,
  "schema": {
    "type": "object",
    "properties": {
      "language": {
        "title": "Language",
        "description": "Language for default tile labels (Swing, Fan Auto, Humidity, etc.). Individual labels can still be overridden per-accessory. Supported: en, ja, zh-CN, zh-TW, ko, de, fr, es, it, pt, nl.",
        "type": "string",
        "default": "en",
        "enum": ["en", "ja", "zh-CN", "zh-TW", "ko", "de", "fr", "es", "it", "pt", "nl"],
        "enumNames": ["English", "日本語", "简体中文", "繁體中文", "한국어", "Deutsch", "Français", "Español", "Italiano", "Português", "Nederlands"]
      },
      "templates": {
        "title": "AC Templates",
        "description": "Named templates shared across accessories. Use {host} and {port} placeholders in URLs. Accessories reference a template by name and only need to supply host/port.",
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name":         { "title": "Template Name",      "description": "Referenced by accessories using the 'template' field.",                      "type": "string", "required": true },
            "stateUrl":     { "title": "State URL",          "description": "Base URL for characteristics with no own URL. Use {host}/{port} placeholders.", "type": "string" },
            "pollInterval": { "title": "Poll Interval (s)",  "description": "How often to refresh state. 0 = disabled.",                                  "type": "integer", "default": 30 },
            "setterDelay":  { "title": "Setter Delay (ms)",  "description": "Debounce delay for SET commands. 0 = immediate.",                            "type": "integer", "default": 0 },
            "minTemp":      { "title": "Min Temperature",    "description": "Minimum target temperature in HomeKit. Default: 16°C.",                      "type": "integer", "default": 16 },
            "maxTemp":      { "title": "Max Temperature",    "description": "Maximum target temperature in HomeKit. Default: 30°C.",                      "type": "integer", "default": 30 },
            "command": {
              "title": "Composed Command",
              "description": "IR-blaster mode: one HTTP call with full AC state. Use {active}, {mode}, {temperature}, {fanSpeed}, {swingVertical}, {swingHorizontal} in body.",
              "type": "object",
              "properties": {
                "url":     { "title": "URL",           "type": "string" },
                "method":  { "title": "Method",        "type": "string", "default": "POST", "enum": ["POST","PUT","PATCH"] },
                "body":    { "title": "Body Template", "description": "JSON body. Placeholders: {active} {mode} {temperature} {fanSpeed} {swingVertical} {swingHorizontal}.", "type": "string" },
                "headers": { "title": "Headers",       "description": "Custom HTTP headers.", "type": "object" },
                "timeout": { "title": "Timeout (ms)",  "type": "integer", "default": 5000 },
                "map":     { "title": "Value Maps",    "description": "Per-field HomeKit→API string maps.", "type": "object" }
              }
            },
            "rotationSpeed": {
              "title": "Fan Speed",
              "description": "RotationSpeed slider, optional Auto switch, or discrete speed buttons.",
              "type": "object",
              "properties": {
                "autoSwitch":      { "title": "Auto Switch",       "description": "Add a linked 'Fan Auto' switch. Auto ON locks slider at 0; slider > 0 turns Auto off.", "type": "boolean" },
                "autoSwitchLabel": { "title": "Auto Switch Label", "description": "Tile label for the Auto switch. Default: 'Fan Auto'. Use your language, e.g. '自動'.", "type": "string" },
                "speeds":          { "title": "Discrete Speeds",   "description": "If set, shows radio-button switches instead of a slider. List speed labels in order.", "type": "array", "items": { "type": "string" } }
              }
            },
            "swingVertical": {
              "title": "Vertical Swing",
              "description": "Maps to HomeKit SwingMode or a linked Switch. Set stateless:true for IR toggle remotes.",
              "type": "object",
              "properties": {
                "stateless": { "title": "Stateless", "description": "Momentary trigger — fires command on press, resets to OFF. No state tracking.", "type": "boolean" },
                "label":     { "title": "Tile Label", "description": "Suffix for the linked tile name. Default: 'Swing'. Use your language, e.g. 'スイング'.", "type": "string" },
                "modes":     { "title": "Swing Modes", "description": "Radio-button labels (stateless only). One Switch per entry; index = value in command map.", "type": "array", "items": { "type": "string" } }
              }
            },
            "swingHorizontal": {
              "title": "Horizontal Swing",
              "description": "Appears as a linked Switch tile.",
              "type": "object",
              "properties": {
                "stateless": { "title": "Stateless", "description": "Momentary trigger — fires command on press, resets to OFF.", "type": "boolean" },
                "label":     { "title": "Tile Label", "description": "Suffix for the linked tile name. Default: 'H-Swing'.", "type": "string" }
              }
            },
            "currentRelativeHumidity": { "title": "Humidity Sensor", "description": "Optional humidity reading. Appears as a linked sensor tile.", "type": "object" }
          }
        }
      },
      "accessories": {
        "title": "AC Units",
        "description": "List of AC accessories. Each can use a template and only override what differs.",
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name":         { "title": "Name",              "description": "Display name in HomeKit.",                          "type": "string", "required": true },
            "serial":       { "title": "Serial Number",     "description": "Used as stable HomeKit UUID seed. Recommended.",   "type": "string" },
            "model":        { "title": "Model",             "description": "Shown in accessory info.",                         "type": "string" },
            "template":     { "title": "Template",          "description": "Name of a platform-level template to inherit.",    "type": "string" },
            "host":         { "title": "Host",              "description": "Replaces {host} in template URLs.",                "type": "string" },
            "port":         { "title": "Port",              "description": "Replaces {port} in template URLs. Default: 80.",   "type": "integer", "default": 80 },
            "stateUrl":     { "title": "State URL",         "description": "Base URL for characteristics that have no own URL. Used as fallback for per-characteristic GET.", "type": "string" },
            "pollInterval": { "title": "Poll Interval (s)", "description": "How often to refresh state from the device. 0 = disabled.",  "type": "integer", "default": 30 },
            "setterDelay":  { "title": "Setter Delay (ms)", "description": "Debounce delay for SET commands. Useful for slider controls. 0 = immediate.", "type": "integer", "default": 0 },
            "minTemp":      { "title": "Min Temperature",   "description": "Minimum target temperature in HomeKit. Default: 16°C.", "type": "integer", "default": 16 },
            "maxTemp":      { "title": "Max Temperature",   "description": "Maximum target temperature in HomeKit. Default: 30°C.", "type": "integer", "default": 30 },
            "command": {
              "title": "Composed Command",
              "description": "IR-blaster / single-shot mode: one HTTP call carrying the full AC state. Use {active}, {mode}, {temperature}, {fanSpeed}, {swingVertical}, {swingHorizontal} in body.",
              "type": "object",
              "properties": {
                "url":     { "title": "URL",    "type": "string" },
                "method":  { "title": "Method", "type": "string", "default": "POST", "enum": ["POST","PUT","PATCH"] },
                "body":    { "title": "Body Template", "description": "JSON body template. Placeholders: {active} {mode} {temperature} {fanSpeed} {swingVertical} {swingHorizontal}.", "type": "string" },
                "headers": { "title": "Headers", "description": "Custom HTTP headers, e.g. {\"Authorization\": \"Bearer token\"}", "type": "object" },
                "timeout": { "title": "Timeout (ms)", "type": "integer", "default": 5000 },
                "map": {
                  "title": "Value Maps",
                  "description": "Per-field mappings from HomeKit values to API strings. Keys are HomeKit values (0/1/2…), values are API strings.",
                  "type": "object"
                }
              }
            },
            "rotationSpeed": {
              "title": "Fan Speed",
              "description": "RotationSpeed slider, optional Auto switch, or discrete speed buttons.",
              "type": "object",
              "properties": {
                "autoSwitch":      { "title": "Auto Switch",       "description": "Add a linked 'Fan Auto' switch. Auto ON locks slider at 0; slider > 0 turns Auto off.", "type": "boolean" },
                "autoSwitchLabel": { "title": "Auto Switch Label", "description": "Tile label for the Auto switch. Default: 'Fan Auto'. Use your language, e.g. '自動'.", "type": "string" },
                "speeds":          { "title": "Discrete Speeds",   "description": "If set, shows radio-button switches instead of a slider. List speed labels in order.", "type": "array", "items": { "type": "string" } }
              }
            },
            "swingVertical": {
              "title": "Vertical Swing",
              "description": "Maps to HomeKit SwingMode or a linked Switch. Set stateless:true for IR toggle remotes.",
              "type": "object",
              "properties": {
                "stateless": { "title": "Stateless", "description": "Momentary trigger — fires command on press, resets to OFF. No state tracking.", "type": "boolean" },
                "label":     { "title": "Tile Label", "description": "Suffix for the linked tile name. Default: 'Swing'. Use your language, e.g. 'スイング'.", "type": "string" },
                "modes":     { "title": "Swing Modes", "description": "Radio-button labels (stateless only). One Switch per entry; index = value in command map.", "type": "array", "items": { "type": "string" } }
              }
            },
            "swingHorizontal": {
              "title": "Horizontal Swing",
              "description": "Appears as a linked Switch tile.",
              "type": "object",
              "properties": {
                "stateless": { "title": "Stateless", "description": "Momentary trigger — fires command on press, resets to OFF.", "type": "boolean" },
                "label":     { "title": "Tile Label", "description": "Suffix for the linked tile name. Default: 'H-Swing'.", "type": "string" }
              }
            },
            "currentRelativeHumidity": { "title": "Humidity Sensor", "description": "Optional humidity reading. Appears as a linked sensor tile inside the AC accessory.", "type": "object" }
          }
        }
      }
    }
  }
}
